# 更新说明

## 0.9.0-rc1 - 2026-08-06

首个面向 Windows 普通用户的候选发布版。

### 新增

- 增加统一的应用版本、构建日期、提交 ID 和运行环境信息。
- “帮助”菜单增加本地更新说明入口，并提供独立“关于”窗口。
- 增加 Windows x64 便携版和安装版构建脚本。
- 增加一键非视觉测试、Swing 烟测、应用 JAR 和发布包构建入口。
- 发布包内置 Java 21 LTS 运行时，用户不需要另行安装 Java。

### 改进

- 打开差异编辑器时先检查文件大小。
- 单侧文件超过 20 MB 时显示风险确认，超过 100 MB 时拒绝进入可编辑差异视图。
- 生产入口中的文件读取、编码检测、解码和首次差异计算改为后台执行。
- 加载过程可取消，失败或取消不会改变主窗口已有对比结果。

### 已知限制

- 首个候选版只支持 Windows 10/11 x64。
- 暂不提供自动联网更新。
- 未签名的候选安装包可能显示 Windows SmartScreen 提示。
- 大文件阈值只保护可编辑差异视图，不限制目录扫描和 SHA-256 对比。

